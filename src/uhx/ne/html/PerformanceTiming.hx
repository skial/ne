package uhx.ne.html;

class PerformanceTiming {
	
	public var connectEnd(default, never):Int;
	public var connectStart(default, never):Int;
	public var domComplete(default, never):Int;
	public var domContentLoadedEventEnd(default, never):Int;
	public var domContentLoadedEventStart(default, never):Int;
	public var domInteractive(default, never):Int;
	public var domLoading(default, never):Int;
	public var domainLookupEnd(default, never):Int;
	public var domainLookupStart(default, never):Int;
	public var fetchStart(default, never):Int;
	public var loadEventEnd(default, never):Int;
	public var loadEventStart(default, never):Int;
	public var navigationStart(default, never):Int;
	public var redirectEnd(default, never):Int;
	public var redirectStart(default, never):Int;
	public var requestStart(default, never):Int;
	public var responseEnd(default, never):Int;
	public var responseStart(default, never):Int;
	public var unloadEventEnd(default, never):Int;
	public var unloadEventStart(default, never):Int;
	
	public function new() {
		
	}
	
}
