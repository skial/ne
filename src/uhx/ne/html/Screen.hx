package uhx.ne.html;

class Screen {
	
	public var availHeight(default, never):Int;
	public var availLeft(default, never):Int;
	public var availTop(default, never):Int;
	public var availWidth(default, never):Int;
	public var colorDepth(default, never):Int;
	public var height(default, never):Int;
	public var left(default, never):Int;
	public var pixelDepth(default, never):Int;
	public var top(default, never):Int;
	public var width(default, never):Int;
	
	public function new() {
		
	}
	
}
